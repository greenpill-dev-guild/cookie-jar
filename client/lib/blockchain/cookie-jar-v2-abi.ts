import { cookieJarFactoryAbi } from "@/generated";

/**
 * Legacy alias retained for backwards compatibility.
 * `client/generated.ts` is the single canonical source for factory ABI.
 */
export const cookieJarFactoryV2Abi = cookieJarFactoryAbi;
