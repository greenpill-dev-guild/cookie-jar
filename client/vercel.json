{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && pnpm check && cd client && pnpm build:skip-lint",
  "installCommand": "cd .. && pnpm install --ignore-scripts",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
